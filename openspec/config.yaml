schema: spec-driven
project: cwa
active_change: establish-cwa-foundation
context: |
  Browser-only Pake MVP. The Wave 0–1 foundation is frozen.
  Sibling changes own compatibility runtime, optional native protocol,
  local tool adapters, and visible-DOM media workflows (TASK-020–059).
