# Contributing

1. Read `START_HERE.md` and `AGENTS.md`.
2. Keep Wave 1 scope: truthful visible-thread export.
3. Run `pnpm test` and `python3 scripts/validate_planning.py`.
4. Do not add native bridges, tool adapters, or media pipelines in this change.
