# Contributing

1. Read `START_HERE.md` and `AGENTS.md`.
2. Preserve the established Waves 1–5 boundaries; new work requires an explicit change.
3. Run `pnpm test`, `python3 scripts/validate_planning.py`, and `python3 scripts/validate_planning.py --runtime`.
4. Do not add or depend on undocumented provider APIs.
