# Roadmap

1. Wave 1 — truthful visible-thread export (done).
2. Wave 2 — compatibility runtime (selectors, scheduler, lifecycle, safe mode, diagnostics) (done).
3. Wave 3 — optional native companion protocol (fail-closed; no sidecar in this repo) (done).
4. Wave 4 — local tool adapters (palette; no default network) (done).
5. Wave 5 — visible-DOM media workflows (file-cards under the same fetch caps) (done).
