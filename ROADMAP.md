# Roadmap

1. Wave 1 — truthful visible-thread export (this change).
2. Wave 2 — compatibility runtime (selectors, lifecycle, scheduler, safe mode).
3. Later OpenSpec changes — native companion, tool adapters, media workflows (separate proposals; not this run).
